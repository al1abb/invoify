import { InvoiceMain } from "@/app/components";

export default function Home() {
    return (
        <main className="py-10 lg:container md:container sm:container-lg sm:py-4 xs:container-lg">
            <InvoiceMain />
        </main>
    );
}
